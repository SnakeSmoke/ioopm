#include <stdio.h>

int main(int argc, char *argv[])
{
  int i=0,j=0,f=0;
  
  for(i=argc; i <= argc; i++){
    
    for(j=1; j <= i; j++){
      printf("#");
	}
    printf("\n");
    f = f+i;
  }
  printf("Total: %d\n ", f);
  return 0;
}
